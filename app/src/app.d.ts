declare global {
    namespace App {
        // interface Error {}
        interface Locals {
            name?: string;
            userId?: string;
        }
        // interface PageData {}
        // interface Platform {}
    }
}

export { };
